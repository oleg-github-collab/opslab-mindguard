# OpsLab Mindguard Platform - Архітектура системи

## 🎯 Огляд системи

Платформа OpsLab Mindguard - це безпечна система моніторингу ментального здоров'я команди з вбудованою "Стіною плачу" для анонімного зворотного зв'язку.

### Ключові особливості:

✅ **Ізоляція даних на рівні БД** - Row Level Security (PostgreSQL)
✅ **Рольова модель доступу** - Admin, Manager, Employee
✅ **Помісячна статистика** з детальними графіками
✅ **Інтеграція Стіни плачу** як розділ платформи
✅ **Telegram-бот** для нагадувань та сповіщень
✅ **Адміністративна панель** для управління користувачами
✅ **Олег виключений з аналітики** (is_excluded_from_analytics)

---

## 🏗️ Архітектура

### Backend Stack
- **FastAPI** - REST API
- **PostgreSQL** - База даних з RLS
- **SQLAlchemy** - ORM
- **Aiogram** - Telegram Bot
- **JWT** - Authentication

### Frontend Stack
- **Vanilla JS** - Мінімалістичний підхід
- **Neobrutal Design** - Існуючий стиль
- **Chart.js / D3.js** - Графіки

### Security
- **Row Level Security (RLS)** - ізоляція даних на рівні БД
- **JWT tokens** - автентифікація
- **Bcrypt** - хешування паролів
- **HTTPS only** - безпечні з'єднання

---

## 📊 Модель даних

### Users (Користувачі)
```sql
- id (PK)
- email (UNIQUE)
- employee_code (UNIQUE, nullable для адміна)
- name
- role: 'admin' | 'manager' | 'employee'
- is_active
- is_excluded_from_analytics (для адміна Олега)
```

### Mental_Health_Metrics (Метрики)
```sql
- id (PK)
- user_id (FK) → users
- assessment_date, month, year
- who5_score (0-100)
- phq9_score (0-27)
- gad7_score (0-21)
- mbi_score (0-100%)
- sleep_duration, sleep_quality
- work_life_balance, stress_level
- risk_level (автоматично): 'low'|'medium'|'high'|'critical'
```

### Wall_Posts (Стіна плачу)
```sql
- id (PK)
- user_id (FK, nullable для анонімності)
- content
- is_anonymous
- category: 'complaint'|'suggestion'|'praise'|'concern'
- sentiment: 'positive'|'neutral'|'negative'
- status: 'open'|'acknowledged'|'resolved'
```

### Admin_Notifications (Сповіщення)
```sql
- id (PK)
- user_id (FK) → users
- type: 'critical_metric'|'new_wall_post'|'weekly_reminder'
- title, message
- related_entity_type, related_entity_id
- is_sent_to_telegram
```

---

## 🔒 Ізоляція даних

### Row Level Security (RLS) Policies

#### Метрики:
- **Employee** - бачить ЛИШЕ свої дані
- **Manager/Admin** - бачить ВСІ дані
- **Олег (admin)** - НЕ відображається в аналітиці

#### Стіна плачу:
- **Всі** - читання всіх постів
- **Користувачі** - створення постів
- **Автор/Admin** - редагування/видалення

### Приклад RLS політики:
```sql
CREATE POLICY employee_view_own_metrics ON mental_health_metrics
    FOR SELECT
    USING (
        user_id = current_setting('app.current_user_id')::INTEGER
        OR EXISTS (
            SELECT 1 FROM users
            WHERE id = current_setting('app.current_user_id')::INTEGER
            AND role IN ('admin', 'manager')
        )
    );
```

---

## 🔐 Рольова модель

### Admin (Олег)
✅ Повний доступ до всіх даних
✅ Редагування списку робітників
✅ Отримання критичних сповіщень
❌ НЕ проходить опитування
❌ НЕ відображається в аналітиці

### Manager (Джейн)
✅ Доступ до всіх метрик команди
✅ Отримання критичних сповіщень
✅ Проходить опитування (відображається в аналітиці)
❌ Не може редагувати користувачів

### Employee
✅ Доступ ЛИШЕ до своїх даних
✅ Проходить опитування
✅ Створює пости на Стіні плачу

---

## 📈 Помісячна статистика

### Метрики для відображення:
- WHO-5 Well-Being Index (тренд)
- PHQ-9 Depression (тренд)
- GAD-7 Anxiety (тренд)
- MBI Burnout % (тренд)
- Sleep Duration + Quality
- Work-Life Balance
- Stress Level (PSS)

### Графіки:
- 📊 **Line Charts** - тренди по місяцях
- 📊 **Bar Charts** - порівняння співробітників
- 📊 **Heatmap** - кореляції метрик
- 📊 **Risk Matrix** - Stress vs Burnout

### Endpoints:
```
GET /api/stats/team/monthly?from=2025-08&to=2025-12
GET /api/stats/employee/{id}/history
GET /api/stats/trends/analysis
```

---

## 🤖 Telegram Bot

### Функції:

#### 1. Щотижневі нагадування (П'ятниця)
```
🗣️ "Привіт, команда! Не забудьте заповнити анкету на Стіні плачу цього тижня."
```

#### 2. Критичні метрики
```
🚨 КРИТИЧНО: У Катерини Петухової критичні показники!
WHO-5: 40, PHQ-9: 16, MBI: 81.5%
Термінова дія необхідна!
```

#### 3. Нові пости на Стіні плачу
```
📝 Новий запис на Стіні плачу
Автор: Анонімний користувач
Категорія: complaint
"[Перші 200 символів контенту...]"
```

### Налаштування:
- **Cron job** для щотижневих нагадувань: `0 10 * * 5` (П'ятниця 10:00)
- **Webhook** для real-time сповіщень
- **Адресати**: Олег + Джейн

---

## 🖥️ Frontend - Адміністративна панель

### Розділи:

#### 1. Dashboard (Головна)
- Командні метрики (поточні)
- Тренди (серпень-грудень)
- Алерти (критичні стани)
- Рекомендації

#### 2. Статистика (NEW!)
- Помісячні графіки для кожної метрики
- Порівняння співробітників
- Експорт в CSV/PDF

#### 3. Співробітники
- Картки з метриками
- Історія по місяцях (sparkline)
- Деталі по кліку

#### 4. Стіна плачу (ІНТЕГРОВАНО)
- Всі пости
- Фільтри: категорія, статус, дата
- Модерація (тільки адмін)

#### 5. Адміністрування (Admin Only)
- Список користувачів
- Додавання/редагування/деактивація
- Скидання паролів

---

## 🔄 Інтеграція Стіни плачу

### Міграція даних:

1. **Витягнути дані** через `fetch_wall_data.py`
2. **Імпортувати** в `wall_posts` таблицю
3. **Мапінг користувачів** по email
4. **Зберегти анонімність** де потрібно

### API Endpoints:

```
GET    /api/wall/posts          # Всі пости
POST   /api/wall/posts          # Новий пост
PATCH  /api/wall/posts/{id}     # Оновити статус
DELETE /api/wall/posts/{id}     # Видалити (admin)
GET    /api/wall/posts/{id}/comments
POST   /api/wall/posts/{id}/comments
```

### Frontend компонент:
```html
<section id="wall-of-complaints">
  <div class="section-title">Стіна плачу</div>
  <div class="post-list">
    <!-- Пости з категоріями, sentiment, статусом -->
  </div>
</section>
```

---

## 🚀 Deployment

### Рекомендації:

- **Backend**: Railway / Fly.io / Heroku
- **Database**: Railway PostgreSQL / Supabase
- **Frontend**: Vercel / Netlify / Railway static
- **Telegram Bot**: Окремий сервіс або разом з backend

### Environment Variables:

```bash
DATABASE_URL=postgresql://...
SECRET_KEY=random-secret-key
TELEGRAM_BOT_TOKEN=123456:ABC...
TELEGRAM_ADMIN_CHAT_ID=12345
TELEGRAM_JANE_CHAT_ID=67890
```

---

## 📋 Чеклист реалізації

### ✅ Виконано:
- [x] Структура БД з RLS
- [x] Python скрипт для витягування даних Стіни плачу
- [x] Backend skeleton (FastAPI)
- [x] Конфігурація

### ⏳ В процесі:
- [ ] Реалізація API endpoints
- [ ] Автентифікація JWT
- [ ] Telegram бот
- [ ] Frontend з помісячною статистикою
- [ ] Адмін панель

### 📝 Наступні кроки:
1. Запустити скрипт витягування даних
2. Створити БД та запустити схему
3. Реалізувати всі API endpoints
4. Інтегрувати Telegram бот
5. Оновити frontend з новими можливостями
6. Тестування ізоляції даних
7. Deployment

---

## 📞 Контакти

**Адміністратори:**
- Олег Камінський: work.olegkaminskyi@gmail.com
- Джейн Давидюк: jane.davydyuk@opslab.uk

**Платформа:** OpsLab Mindguard
**Версія:** 1.0.0 (2026)
