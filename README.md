# 🧠 OpsLab Mindguard Platform

> **Платформа моніторингу ментального здоров'я команди з вбудованою системою анонімного зворотного зв'язку**

## ✅ Production Ready (2026-01-04)

**Статус:** 🟢 Всі критичні проблеми виправлені - готово до деплою!

📖 **Швидкий старт:** [DEPLOYMENT.md](DEPLOYMENT.md)
🔧 **Технічні деталі:** [FIXES_SUMMARY.md](FIXES_SUMMARY.md)

---

## 🎯 Особливості

✅ **Щоденні чекіни** - автоматична розсилка о 10:00 AM через Telegram
✅ **32+ варіацій питань** - різні формулювання для підтримки інтересу
✅ **Повна ізоляція даних** - співробітники бачать лише свої метрики
✅ **Рольовий доступ** - Admin, Manager, Employee
✅ **Помісячна статистика** з детальними трендами
✅ **Стіна плачу** - анонімний зворотний зв'язок
✅ **Критичні алерти** - автоматичні сповіщення при ризикових показниках
✅ **Voice AI аналіз** - транскрипція + емоційний аналіз через OpenAI
✅ **Адмін панель** - управління користувачами
✅ **Автоматичний розрахунок ризиків** - критичні стани визначаються автоматично

---

## 📊 Метрики, що відстежуються

| Метрика | Опис | Діапазон | Критичні значення |
|---------|------|----------|-------------------|
| **WHO-5** | Індекс благополуччя | 0-100 | < 50 (ризик), < 28 (критично) |
| **PHQ-9** | Депресія | 0-27 | ≥ 10 (помірна), ≥ 15 (тяжка) |
| **GAD-7** | Тривожність | 0-21 | ≥ 10 (помірна), ≥ 15 (тяжка) |
| **MBI** | Вигорання | 0-100% | ≥ 50% (високе), ≥ 70% (критичне) |
| **Sleep** | Сон | 0-24 год | < 6 год (недостатньо) |
| **WLB** | Work-Life Balance | 0-10 | < 5 (незадовільно) |
| **Stress** | Рівень стресу (PSS) | 0-40 | ≥ 26 (високий) |

---

## 🏗️ Архітектура

### Backend
- **Rust + Tokio** - Async runtime (швидкість + надійність)
- **Axum** - Web framework для HTTP API
- **PostgreSQL** - База даних з Row Level Security
- **SQLx** - Async database driver + міграції
- **Teloxide** - Telegram Bot framework
- **OpenAI API** - Voice transcription (Whisper) + AI analysis
- **AES-256-GCM** - Шифрування sensitive data
- **Argon2** - Password hashing

### Frontend
- **Vanilla JS** - Без фреймворків
- **Neobrutal Design** - Мінімалістичний дизайн
- **Chart.js** - Графіки та візуалізації

### Automation
- **tokio-cron-scheduler** - Щоденні чекіни о 10:00 AM
- **Session Management** - In-memory HashMap для швидкості
- **Auto-cleanup** - Hourly session cleanup

### Security
- **Row Level Security (RLS)** - ізоляція на рівні БД
- **Argon2** - хешування паролів (crypto-safe)
- **AES-256-GCM** - шифрування voice transcripts
- **HTTPS** - всі з'єднання
- **Rate Limiting** - Telegram API protection

---

## 🚀 Швидкий старт

### 1. Клонувати репозиторій
```bash
git clone https://github.com/opslab/mindguard-platform.git
cd mindguard-platform
```

### 2. Налаштувати базу даних
```bash
createdb opslab_mindguard
psql -d opslab_mindguard -f backend/database_schema.sql
```

### 3. Налаштувати backend
```bash
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Створити .env
cp .env.example .env
# Відредагувати .env з вашими даними
```

### 4. Запустити сервер
```bash
uvicorn main:app --reload
```

### 5. Відкрити в браузері
```
http://localhost:8000
```

---

## 📁 Структура проекту

```
opslab-mindguard/
├── backend/
│   ├── main.py                 # FastAPI додаток
│   ├── config.py               # Конфігурація
│   ├── database_schema.sql     # Схема БД з RLS
│   ├── telegram_bot.py         # Telegram бот
│   ├── requirements.txt        # Залежності
│   ├── .env.example            # Приклад змінних середовища
│   ├── routers/                # API роутери
│   ├── models/                 # SQLAlchemy моделі
│   └── utils/                  # Утиліти
├── scraper/
│   └── fetch_wall_data.py      # Скрипт витягування даних
├── index.html                  # Frontend (єдиний файл)
├── static/
│   └── style.css               # Neobrutal стилі
├── ARCHITECTURE.md             # Детальна архітектура
├── IMPLEMENTATION_GUIDE.md     # Покрокова інструкція
└── README.md                   # Цей файл
```

---

## 🔒 Безпека та ізоляція даних

### Row Level Security (PostgreSQL)

Дані ізольовані на рівні БД через RLS політики:

```sql
-- Співробітники бачать ЛИШЕ свої дані
CREATE POLICY employee_view_own_metrics ON mental_health_metrics
    FOR SELECT
    USING (
        user_id = current_setting('app.current_user_id')::INTEGER
        OR EXISTS (
            SELECT 1 FROM users
            WHERE id = current_setting('app.current_user_id')::INTEGER
            AND role IN ('admin', 'manager')
        )
    );
```

### Ролі

| Роль | Доступ до даних | Можливості |
|------|----------------|------------|
| **Admin** (Олег) | Всі дані | Редагування користувачів, НЕ в аналітиці |
| **Manager** (Джейн) | Всі дані | Перегляд команди, сповіщення |
| **Employee** | Лише свої | Проходження опитувань |

---

## 🤖 Telegram Bot

### Налаштування

1. Створіть бота через [@BotFather](https://t.me/BotFather)
2. Отримайте token
3. Додайте в `.env`:
```env
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
TELEGRAM_ADMIN_CHAT_ID=12345678
TELEGRAM_JANE_CHAT_ID=87654321
```

### Запуск бота
```bash
python backend/telegram_bot.py
```

### Функції

- 🚨 **Критичні алерти** - автоматично при виявленні критичних метрик
- 📝 **Нові пости** - сповіщення коли хтось додає на Стіну плачу
- 🗓️ **Щотижневі нагадування** - кожної п'ятниці о 10:00

### Cron job для нагадувань

```bash
crontab -e

# Додати:
0 10 * * 5 cd /path/to/backend && /path/to/venv/bin/python -c "from telegram_bot import send_weekly_reminder; import asyncio; asyncio.run(send_weekly_reminder())"
```

---

## 📊 API Endpoints

### Автентифікація
```
POST   /api/auth/login          # Вхід
POST   /api/auth/logout         # Вихід
GET    /api/auth/me             # Поточний користувач
```

### Метрики
```
GET    /api/metrics/my          # Мої метрики
GET    /api/metrics/team/all    # Всі метрики (admin/manager)
GET    /api/metrics/monthly     # Помісячна статистика
POST   /api/metrics             # Додати метрики
```

### Стіна плачу
```
GET    /api/wall/posts          # Всі пости
POST   /api/wall/posts          # Новий пост
PATCH  /api/wall/posts/{id}     # Оновити статус
DELETE /api/wall/posts/{id}     # Видалити (admin)
```

### Адміністрування
```
GET    /api/admin/users         # Список користувачів
POST   /api/admin/users         # Додати користувача
PATCH  /api/admin/users/{id}    # Редагувати
DELETE /api/admin/users/{id}    # Деактивувати
```

### Статистика
```
GET    /api/stats/trends        # Тренди команди
GET    /api/stats/correlations  # Кореляції метрик
GET    /api/stats/risks         # Аналіз ризиків
```

---

## 🎨 Frontend

### Розділи

1. **Dashboard** - Огляд команди
2. **Статистика** - Помісячні графіки
3. **Співробітники** - Картки з метриками
4. **Стіна плачу** - Анонімний зворотний зв'язок
5. **Адмін панель** - Управління (лише admin)

### Стилі

Neobrutal design з:
- Жирними чорними обрисами
- Помаранчевими акцентами
- Тінями
- Мінімалістичними картками

---

## 🧪 Тестування

### Перевірка ізоляції даних

```bash
# Увійдіть як співробітник
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "employee@opslab.uk", "password": "0000"}'

# Спробуйте отримати чужі дані (має відмовити):
curl http://localhost:8000/api/metrics/team/all \
  -H "Authorization: Bearer $TOKEN"
# Очікується: 403 Forbidden
```

### Перевірка адміна

```bash
# Увійдіть як Олег
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "work.olegkaminskyi@gmail.com", "password": "admin_password"}'

# Має отримати ВСІ дані:
curl http://localhost:8000/api/metrics/team/all \
  -H "Authorization: Bearer $TOKEN"
```

---

## 📦 Deployment

### Railway (рекомендовано)

```bash
railway login
railway init
railway add postgresql
railway up
```

### Docker

```bash
docker-compose up -d
```

### Environment Variables

```env
DATABASE_URL=postgresql://...
SECRET_KEY=your-secret-key
TELEGRAM_BOT_TOKEN=...
TELEGRAM_ADMIN_CHAT_ID=...
TELEGRAM_JANE_CHAT_ID=...
```

---

## 📝 Міграція даних зі Стіни плачу

```bash
cd scraper
python fetch_wall_data.py
# Дані збережуться в wall_data_extracted.json
# Імпортуйте в БД через API або SQL
```

---

## 🛠️ Підтримка

### Логи

```bash
tail -f backend/logs/app.log
```

### БД перевірка

```bash
psql -d opslab_mindguard -c "SELECT * FROM users;"
```

### API документація

```
http://localhost:8000/api/docs
```

---

## 👥 Команда

- **Олег Камінський** - Admin (work.olegkaminskyi@gmail.com)
- **Джейн Давидюк** - Manager (jane.davydyuk@opslab.uk)

---

## 📄 Ліцензія

© 2026 OpsLab. All rights reserved.

---

## 🔗 Додаткові ресурси

- [Детальна архітектура](ARCHITECTURE.md)
- [Інструкція з впровадження](IMPLEMENTATION_GUIDE.md)
- [API Documentation](http://localhost:8000/api/docs)

---

**Made with 🧡 by OpsLab**
